<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.linn.home.dao.NoticeDao">
    <resultMap id="noticeResultMap" type="com.linn.home.entity.Notice">
        <id property="id" column="id" />
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="author" column="author"/>
        <result property="isActice" column="is_actice"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModified" column="gmt_modified"/>
    </resultMap>

    <select id="findActiveNotice" resultMap="noticeResultMap">
        select * from blog_notice where is_active = 1;
    </select>

    <select id="findNoticeList" resultMap="noticeResultMap">
        select * from blog_notice;
    </select>

    <insert id="addNotice"  keyProperty="id">
        INSERT INTO blog_notice (title, content,author,isActice)
        VALUES (#{title}, #{content},#{author},#{isActice})
    </insert>

    <update id="updateNoticeById">
        UPDATE blog_notice
        SET title  = #{title}, content = #{content},author = #{author},is_actice = #{isActice}, gmt_modified = #{gmtModified}
        WHERE id = #{id}
    </update>

    <delete id="deleteNoticeById">
        delete from blog_notice where id = #{id}
    </delete>
</mapper>